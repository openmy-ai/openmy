---
phase: 01-evidence-based-audio-mapping
plan: 03
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - tests/unit/test_transcription_enrichment.py
  - tests/unit/test_app_server.py
autonomous: true
requirements:
  - LINK-01
  - LINK-03
user_setup: []
must_haves:
  truths:
    - Repeated scene text and later correction rewrites do not change evidence-derived audio ownership
    - Browser-facing day detail payloads remain readable when audio_ref exists or is absent
    - Existing scene rewrite paths preserve audio_ref when they do not own playback provenance
  artifacts:
    - tests/unit/test_transcription_enrichment.py
    - tests/unit/test_app_server.py
  key_links:
    - correction and day-detail regressions assert on stable audio_ref behavior
    - payload readers treat missing audio_ref as compatible historical data
---

<objective>
Close Phase 1 with regression coverage for the exact failure modes that triggered the engineering review.

Purpose: Prevent the rejected text-based approach from sneaking back in through later edits.
Output: Repeated-text, corrected-text, and browser-payload regression tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-evidence-based-audio-mapping/01-CONTEXT.md
@.planning/phases/01-evidence-based-audio-mapping/01-RESEARCH.md
@.planning/phases/01-evidence-based-audio-mapping/01-PATTERNS.md
@tests/unit/test_transcription_enrichment.py
@tests/unit/test_app_server.py
@app/payloads.py
@src/openmy/commands/show.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add repeated-text and corrected-text provenance regressions</name>
  <files>tests/unit/test_transcription_enrichment.py, tests/unit/test_app_server.py</files>
  <read_first>tests/unit/test_transcription_enrichment.py, tests/unit/test_app_server.py, src/openmy/services/ingest/transcription_enrichment.py, app/payloads.py</read_first>
  <action>Add regression coverage that proves audio ownership is evidence-derived rather than text-derived. One test should cover repeated text that would be ambiguous under reverse matching but still yields the right result from evidence. Another should cover a correction rewrite path and assert that text replacement does not erase or mutate an existing `audio_ref` field.</action>
  <verify>python3 -m pytest tests/unit/test_transcription_enrichment.py tests/unit/test_app_server.py -k "audio_ref or correction" -v</verify>
  <acceptance_criteria>
    - tests/unit/test_transcription_enrichment.py contains "repeated_text" or "repeated text"
    - tests/unit/test_app_server.py contains "audio_ref" and "correction"
    - tests/unit/test_app_server.py contains a negative assertion that corrected text alone does not control playback provenance
  </acceptance_criteria>
  <done>Regression tests encode the review’s main correctness concerns.</done>
</task>

<task type="auto">
  <name>Task 2: Add browser-facing compatibility tests for mapped and unmapped scenes</name>
  <files>tests/unit/test_app_server.py</files>
  <read_first>tests/unit/test_app_server.py, app/payloads.py, src/openmy/commands/show.py</read_first>
  <action>Extend server or payload tests so day detail responses are checked in two modes: scenes with `audio_ref` present and historical scenes without it. Also add coverage that `freeze_scene_roles(...)` or equivalent scene rewrite paths preserve unknown fields like `audio_ref` when they touch role data only.</action>
  <verify>python3 -m pytest tests/unit/test_app_server.py -k "audio_ref or date_detail or correction" -v</verify>
  <acceptance_criteria>
    - tests/unit/test_app_server.py contains "test_get_date_detail_preserves_audio_ref_in_scenes"
    - tests/unit/test_app_server.py contains "test_get_date_detail_allows_missing_audio_ref"
    - tests/unit/test_app_server.py contains "test_handle_correction_preserves_audio_ref" or equivalent explicit preservation coverage
  </acceptance_criteria>
  <done>Browser-facing payload behavior is locked for both new and historical data.</done>
</task>

<task type="auto">
  <name>Task 3: Run the combined Phase 1 regression suite</name>
  <files>tests/unit/test_transcription_enrichment.py, tests/unit/test_app_server.py</files>
  <read_first>.planning/phases/01-evidence-based-audio-mapping/01-01-PLAN.md, .planning/phases/01-evidence-based-audio-mapping/01-02-PLAN.md</read_first>
  <action>Run the targeted regression suite for Phase 1 and only finish if the helper, pipeline, and payload tests all pass together. If any test fails because the code still assumes text-based ownership, adjust the implementation inside the already-planned files instead of broadening scope.</action>
  <verify>python3 -m pytest tests/unit/test_transcription_enrichment.py tests/unit/test_cli.py tests/unit/test_app_server.py -k "audio_ref or transcription_enrich or correction or date_detail" -v</verify>
  <acceptance_criteria>
    - The combined pytest command above exits 0
    - tests/unit/test_transcription_enrichment.py, tests/unit/test_cli.py, and tests/unit/test_app_server.py all contain "audio_ref"
  </acceptance_criteria>
  <done>Phase 1 ends with a single regression suite that protects the accepted architecture.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python3 -m pytest tests/unit/test_transcription_enrichment.py tests/unit/test_cli.py tests/unit/test_app_server.py -k "audio_ref or transcription_enrich or correction or date_detail" -v`
- [ ] `rg -n "audio_ref" tests/unit/test_transcription_enrichment.py tests/unit/test_cli.py tests/unit/test_app_server.py`
- [ ] Confirm browser payload tests cover both mapped and unmapped scene data
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 1 ends with regression coverage for repeated text, corrected text, and historical no-ref compatibility
  </success_criteria>

<output>
After completion, create `.planning/phases/01-evidence-based-audio-mapping/01-03-SUMMARY.md`
</output>
