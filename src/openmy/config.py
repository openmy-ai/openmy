"""OpenMy 全局配置 — 模型、超时等统一管理"""

# ── 模型配置 ──────────────────────────────────────────────
# 改这一个地方，所有模块（清洗/蒸馏/提取/转写）都会跟着变
GEMINI_MODEL = "gemini-2.5-flash"

# ── 超时配置 ──────────────────────────────────────────────
GEMINI_CLI_TIMEOUT = 300        # Gemini CLI 调用超时（秒）
TRANSCRIBE_TIMEOUT = 900        # 转写超时（秒），音频文件大所以更长
