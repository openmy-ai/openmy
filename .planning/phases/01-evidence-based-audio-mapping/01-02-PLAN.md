---
phase: 01-evidence-based-audio-mapping
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/openmy/commands/run.py
  - tests/unit/test_cli.py
autonomous: true
requirements:
  - LINK-01
  - LINK-03
user_setup: []
must_haves:
  truths:
    - Successful enrichment updates scene artifacts with audio references during both fresh and reused-scene run paths
    - Run orchestration no longer requires a manual align-only gate to write scene audio references
    - Days without playable refs still complete the main pipeline safely
  artifacts:
    - src/openmy/commands/run.py
    - tests/unit/test_cli.py
  key_links:
    - run.py invokes scene enrichment writeback based on available enrichment data
    - pipeline tests assert scenes.json contains or safely omits audio_ref
---

<objective>
Thread the new scene audio reference contract through the day-run orchestration paths that actually build or reuse `scenes.json`.

Purpose: A correct helper is not enough if the run pipeline never calls it at the right time.
Output: Run-path integration plus CLI regression tests for fresh and reused artifacts.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-evidence-based-audio-mapping/01-CONTEXT.md
@.planning/phases/01-evidence-based-audio-mapping/01-RESEARCH.md
@.planning/phases/01-evidence-based-audio-mapping/01-PATTERNS.md
@.planning/phases/01-evidence-based-audio-mapping/01-01-PLAN.md
@src/openmy/commands/run.py
@tests/unit/test_cli.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the align-only gate for scene enrichment writeback</name>
  <files>src/openmy/commands/run.py</files>
  <read_first>src/openmy/commands/run.py, src/openmy/services/ingest/transcription_enrichment.py, .planning/phases/01-evidence-based-audio-mapping/01-01-PLAN.md</read_first>
  <action>Refactor the two scene-processing branches in `run.py` so `apply_transcription_enrichment_to_scenes(...)` is triggered from actual enrichment availability or reusable transcription payload state, not only from `stt_align`. Keep optional enrichment failures non-blocking for the main chain, and preserve the existing step/status reporting style.</action>
  <verify>python3 -m pytest tests/unit/test_cli.py -k "audio_ref or transcription_enrich" -v</verify>
  <acceptance_criteria>
    - src/openmy/commands/run.py contains "apply_transcription_enrichment_to_scenes"
    - src/openmy/commands/run.py does not gate both call sites solely with "stt_align"
    - src/openmy/commands/run.py still contains "_mark_step" updates for the enrichment-related flow
  </acceptance_criteria>
  <done>The run pipeline writes scene audio refs whenever enrichment data is genuinely available.</done>
</task>

<task type="auto">
  <name>Task 2: Add CLI regression tests for fresh-run and reused-scene paths</name>
  <files>tests/unit/test_cli.py</files>
  <read_first>tests/unit/test_cli.py, src/openmy/commands/run.py, .planning/phases/01-evidence-based-audio-mapping/01-PATTERNS.md</read_first>
  <action>Add or extend CLI tests so one case covers a fresh run that ends with `scenes.json` containing `audio_ref`, and another covers an existing `scenes.json` reuse path that safely preserves or omits `audio_ref` without failure. Use temp day-dir fixtures and assert directly on written JSON artifacts.</action>
  <verify>python3 -m pytest tests/unit/test_cli.py -k "audio_ref or transcription_enrich" -v</verify>
  <acceptance_criteria>
    - tests/unit/test_cli.py contains "test_cmd_run_applies_audio_ref_after_enrichment"
    - tests/unit/test_cli.py contains "test_cmd_run_reused_scenes_preserve_or_skip_audio_ref_safely"
    - tests/unit/test_cli.py contains "audio_ref"
  </acceptance_criteria>
  <done>CLI tests prove the orchestration path writes scene audio refs in real pipeline flows.</done>
</task>

<task type="auto">
  <name>Task 3: Verify safe behavior when refs are unavailable</name>
  <files>tests/unit/test_cli.py</files>
  <read_first>tests/unit/test_cli.py, .planning/phases/01-evidence-based-audio-mapping/01-CONTEXT.md</read_first>
  <action>Make one CLI regression explicitly assert the safe-fail case: the day run still completes and `scenes.json` remains readable when `audio_ref` cannot be derived. Tie the assertion to missing or ambiguous evidence rather than to text content so later correction rewrites cannot break the test's intent.</action>
  <verify>python3 -m pytest tests/unit/test_cli.py -k "audio_ref or transcription_enrich" -v</verify>
  <acceptance_criteria>
    - tests/unit/test_cli.py contains "audio_ref" in both positive and negative assertions
    - tests/unit/test_cli.py contains an assertion that run status stays completed or non-failed when audio_ref is absent
  </acceptance_criteria>
  <done>The pipeline contract is locked: lack of playable refs is a safe compatibility state, not a pipeline failure.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python3 -m pytest tests/unit/test_cli.py -k "audio_ref or transcription_enrich" -v`
- [ ] `rg -n "apply_transcription_enrichment_to_scenes|audio_ref" src/openmy/commands/run.py tests/unit/test_cli.py`
- [ ] Confirm run-path tests cover both fresh and reused scene flows
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The run pipeline reliably writes or safely omits scene audio refs without depending on text matching
  </success_criteria>

<output>
After completion, create `.planning/phases/01-evidence-based-audio-mapping/01-02-SUMMARY.md`
</output>
