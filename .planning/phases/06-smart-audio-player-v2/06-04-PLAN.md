---
phase: 06-smart-audio-player-v2
plan: 04
type: execute
wave: 2
depends_on: [02, 03]
files_modified:
  - app/static/modules/subtitle-overlay.js
  - app/static/css/subtitle-overlay.css
  - app/static/modules/corrections.js
  - app/index.html
requirements:
  - SAP-04
  - SAP-05
  - SAP-06
autonomous: true
user_setup: []
must_haves:
  truths:
    - 播放时文字像字幕流动（当前句高亮 + 自动卷动 + 过渡动画）
    - subtitle-overlay 包含波形组件
    - 来源原句显示命中句而非整段场景原文
    - 句子列表点击跳到对应 VAD 段附近播放
    - 显示原文按钮改名为显示原文和播放原声
  artifacts:
    - app/static/modules/subtitle-overlay.js
    - app/static/css/subtitle-overlay.css
    - app/static/modules/corrections.js
  key_links:
    - subtitle-overlay 整合波形组件
    - speech_segments 驱动句子 ↔ 时间映射
    - corrections.js 的 extractMatchedSentence 驱动命中句锚点
---

<objective>
重构字幕回看 overlay 为"字幕动画播放器"：集成波形组件，实现字幕流动动画，将"显示原文"改为"显示原文和播放原声"。

这是用户最核心的体验需求：边听边看字幕滚动，像看字幕一样回顾录音。
</objective>

<context>
@.planning/phases/06-smart-audio-player-v2/06-CONTEXT.md
@app/static/modules/subtitle-overlay.js
@app/static/modules/corrections.js
@app/static/modules/waveform.js
@app/static/css/subtitle-overlay.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: corrections.js — 补命中句锚点</name>
  <files>app/static/modules/corrections.js</files>
  <read_first>
    - app/static/modules/corrections.js（看 buildCorrectionAnchor 和 SELECTION_TARGET_SELECTOR）
  </read_first>
  <action>
在 buildCorrectionAnchor 之前新增 extractMatchedSentence 函数：

```javascript
function extractMatchedSentence(targetEl, selectedText = '') {
  if (!targetEl || !selectedText) return '';
  const block = targetEl.closest(SELECTION_TARGET_SELECTOR);
  if (!block) return '';
  const fullText = plainText(block.textContent || '');
  if (!fullText) return '';
  const sentences = fullText.match(/[^。！？；\n]+[。！？；]?/g) || [fullText];
  const needle = plainText(selectedText);
  const hit = sentences.find((s) => s.includes(needle));
  if (hit) return hit.trim();
  const compact = needle.replace(/\s+/g, '');
  const fuzzy = sentences.find((s) => s.replace(/\s+/g, '').includes(compact));
  return fuzzy ? fuzzy.trim() : '';
}
```

在 buildCorrectionAnchor 的 return 对象里加 `matchedSentence: extractMatchedSentence(targetEl, selectedText)`。
  </action>
  <acceptance_criteria>
    - corrections.js 包含 `function extractMatchedSentence(`
    - buildCorrectionAnchor return 包含 `matchedSentence:`
    - `node --check app/static/modules/corrections.js` 退出码 0
  </acceptance_criteria>
  <verify>node --check app/static/modules/corrections.js && rg -n "extractMatchedSentence\|matchedSentence" app/static/modules/corrections.js</verify>
</task>

<task type="auto">
  <name>Task 2: subtitle-overlay.js — 重构 buildReviewModel</name>
  <files>app/static/modules/subtitle-overlay.js</files>
  <read_first>
    - app/static/modules/subtitle-overlay.js（全文）
    - app/static/modules/waveform.js（看导出的函数签名）
  </read_first>
  <action>
重构 buildReviewModel：

1. 新增 matchedSentence、hasExactMatch、speechSegments 字段
2. 从 scene.audio_ref 读取 speech_segments 和 duration_seconds

```javascript
const matchedSentence = anchor.matchedSentence || '';
const speechSegments = scene?.transcription_evidence?.[0]?.audio_ref?.speech_segments
  || target?.audioRef?.speech_segments || [];
const realDuration = target?.audioRef?.duration_seconds || 0;

return {
  anchor, scene, target, sourceText,
  matchedSentence,
  hasExactMatch: Boolean(matchedSentence),
  speechSegments,
  realDuration,
  sentences: buckets,
  currentIndex,
  hasAudio: Boolean(target?.audioRef?.chunk_id && target?.url),
};
```
  </action>
  <acceptance_criteria>
    - buildReviewModel 返回包含 `speechSegments` 和 `realDuration`
    - `node --check app/static/modules/subtitle-overlay.js` 退出码 0
  </acceptance_criteria>
  <verify>rg -n "speechSegments\|realDuration\|matchedSentence" app/static/modules/subtitle-overlay.js</verify>
</task>

<task type="auto">
  <name>Task 3: subtitle-overlay.js — 用 VAD 段映射句子时间</name>
  <files>app/static/modules/subtitle-overlay.js</files>
  <read_first>
    - app/static/modules/subtitle-overlay.js（看 buildSentenceBuckets）
  </read_first>
  <action>
重写 buildSentenceBuckets，当有 speechSegments 时用 VAD 段映射，否则均匀分布：

```javascript
function buildSentenceBuckets(sentences, target, selectedText, speechSegments = [], realDuration = 0) {
  const matchedIndex = Math.max(0, findMatchedSentenceIndex(sentences, selectedText));
  const hasTiming = speechSegments.length > 0 && realDuration > 0;

  return sentences.map((text, index) => {
    let startTime = 0;
    let endTime = 0;

    if (hasTiming) {
      // 将句子均匀映射到 VAD 语音段
      const segIndex = Math.min(index, speechSegments.length - 1);
      const seg = speechSegments[segIndex];
      if (seg) {
        startTime = seg.start;
        endTime = seg.end;
      }
    } else if (realDuration > 0) {
      // 无 VAD 但有时长，均匀分布
      const slice = realDuration / sentences.length;
      startTime = index * slice;
      endTime = startTime + slice;
    }

    return {
      index,
      label: `句 ${String(index + 1).padStart(2, '0')}`,
      text,
      matched: index === matchedIndex,
      startTime: Math.round(startTime * 10) / 10,
      endTime: Math.round(endTime * 10) / 10,
      hasTiming,
    };
  });
}
```
  </action>
  <acceptance_criteria>
    - buildSentenceBuckets 接受 speechSegments 和 realDuration 参数
    - 有 VAD 数据时句子有真实 startTime/endTime
    - 无 VAD 数据时降级为均匀分布
  </acceptance_criteria>
  <verify>rg -A 5 "function buildSentenceBuckets" app/static/modules/subtitle-overlay.js</verify>
</task>

<task type="auto">
  <name>Task 4: subtitle-overlay.js — 集成波形组件到 render</name>
  <files>app/static/modules/subtitle-overlay.js</files>
  <read_first>
    - app/static/modules/subtitle-overlay.js（看 renderSubtitleReview 函数的 HTML 输出）
    - app/static/modules/waveform.js
  </read_first>
  <action>
在 renderSubtitleReview 的 HTML 中：

1. 替换 `<input class="subtitle-progress-range" type="range">` 为波形容器：

```html
<div class="waveform-container" id="subtitle-waveform-container">
  <canvas class="waveform-canvas" id="subtitle-waveform-canvas"></canvas>
  <span class="waveform-time" id="subtitle-waveform-time">0:00 / 0:00</span>
  <span class="waveform-loading" id="subtitle-waveform-loading">加载波形中...</span>
</div>
```

2. 在 render 完成后初始化波形：

```javascript
// 在 innerHTML 赋值后
const canvas = document.getElementById('subtitle-waveform-canvas');
const timeDisplay = document.getElementById('subtitle-waveform-time');
const loadingEl = document.getElementById('subtitle-waveform-loading');

if (canvas && refreshed.hasAudio && refreshed.target?.url) {
  fetchWaveformData(refreshed.target.url).then(({waveform, duration}) => {
    if (loadingEl) loadingEl.style.display = 'none';
    renderWaveform(canvas, waveform, duration, refreshed.speechSegments, audio?.currentTime || 0);
    bindWaveformSeek(canvas, duration, (time) => {
      const audio = ensureSceneAudio();
      audio.currentTime = refreshed.target.start + time;
    });
  });
}
```

3. 在 audio timeupdate 中刷新波形进度。

4. 来源原句区域改为显示命中句

5. 播放按钮文案改为"回听整段录音"

6. 进度提示改为"点击下方句子可以浏览全文"
  </action>
  <acceptance_criteria>
    - renderSubtitleReview 的 HTML 包含 `waveform-container`
    - 不再包含 `subtitle-progress-range`
    - 包含 `fetchWaveformData` 调用
    - 包含 `命中原句` 文案
  </acceptance_criteria>
  <verify>rg -n "waveform-container\|fetchWaveformData\|命中原句\|subtitle-progress-range" app/static/modules/subtitle-overlay.js</verify>
</task>

<task type="auto">
  <name>Task 5: subtitle-overlay.js — 字幕流动动画</name>
  <files>app/static/modules/subtitle-overlay.js</files>
  <read_first>
    - app/static/modules/subtitle-overlay.js（看 focusSubtitleSentence 和 renderSubtitleReview 的句子列表渲染）
  </read_first>
  <action>
1. 在 audio timeupdate 事件中，根据当前播放时间自动切换高亮句子：

```javascript
// 在 openSubtitleReview 中注册 timeupdate 监听
const audio = ensureSceneAudio();
audio.addEventListener('timeupdate', () => {
  const model = buildReviewModel();
  if (!model) return;
  const relativeTime = audio.currentTime - (model.target?.start || 0);
  const matchedBucket = model.sentences.find(
    (s) => s.hasTiming && relativeTime >= s.startTime && relativeTime < s.endTime
  );
  if (matchedBucket && matchedBucket.index !== state.subtitleReview.selectedSentenceIndex) {
    state.subtitleReview.selectedSentenceIndex = matchedBucket.index;
    // 只更新高亮和滚动，不重绘整个面板
    updateSentenceHighlight(matchedBucket.index);
  }
  // 刷新波形进度
  updateWaveformProgress(relativeTime);
});
```

2. 新增 `updateSentenceHighlight(index)` 函数：
- 移除所有 `.is-active` 类
- 给当前句添加 `.is-active`
- 滚动到可见位置（smooth）

3. 新增 `updateWaveformProgress(time)` 函数：
- 重绘波形进度线位置

4. 句子列表的 CSS 过渡：
```css
.subtitle-sentence {
  transition: background-color 0.3s ease, transform 0.2s ease;
}
.subtitle-sentence.is-active {
  transform: scale(1.02);
}
```
  </action>
  <acceptance_criteria>
    - 播放时句子自动切换高亮
    - 当前句平滑滚动到可见
    - 波形进度线实时更新
    - 有过渡动画效果
  </acceptance_criteria>
  <verify>rg -n "timeupdate\|updateSentenceHighlight\|updateWaveformProgress" app/static/modules/subtitle-overlay.js</verify>
</task>

<task type="auto">
  <name>Task 6: focusSubtitleSentence — 点击跳到 VAD 段播放</name>
  <files>app/static/modules/subtitle-overlay.js</files>
  <read_first>
    - app/static/modules/subtitle-overlay.js（看当前 focusSubtitleSentence 实现）
  </read_first>
  <action>
改造 focusSubtitleSentence：有 VAD 数据时跳播，无数据时只切高亮：

```javascript
export async function focusSubtitleSentence(index) {
  const model = buildReviewModel();
  if (!model) return;
  const nextIndex = Math.max(0, Math.min(model.sentences.length - 1, Number(index || 0)));
  state.subtitleReview.selectedSentenceIndex = nextIndex;

  const bucket = model.sentences[nextIndex];
  if (bucket?.hasTiming && model.hasAudio) {
    // 有 VAD 时间 → 跳播
    const audio = ensureSceneAudio();
    try {
      await prepareScenePlayback(model.target);
      audio.currentTime = model.target.start + bucket.startTime;
      await audio.play();
    } catch (e) {
      // 播放失败不影响高亮
    }
  }

  renderSubtitleReview();
  const activeEl = document.querySelector('.subtitle-sentence.is-active');
  if (activeEl) activeEl.scrollIntoView({ behavior: 'smooth', block: 'nearest' });
}
```
  </action>
  <acceptance_criteria>
    - 有 VAD 数据时 focusSubtitleSentence 跳到 startTime 播放
    - 无 VAD 数据时只切高亮不跳播
    - 包含 `bucket.startTime` 引用
  </acceptance_criteria>
  <verify>rg -A 20 "export async function focusSubtitleSentence" app/static/modules/subtitle-overlay.js</verify>
</task>

<task type="auto">
  <name>Task 7: UI — 显示原文按钮改名</name>
  <files>app/index.html, app/static/modules/subtitle-overlay.js</files>
  <read_first>
    - app/index.html（搜索"显示原文"按钮）
    - app/static/modules/subtitle-overlay.js（搜索相关文案）
  </read_first>
  <action>
1. 在 index.html 或 JS 中将"显示原文"改为"显示原文和播放原声"
2. 回看 overlay 里的"播放原声"改为"回听整段录音"
3. 删掉旧的 range input 时间轴（如果还没在 Task 4 中删掉的话）
  </action>
  <acceptance_criteria>
    - 不再出现"播放原声"（改为"回听整段录音"）
    - 出现"显示原文和播放原声"
  </acceptance_criteria>
  <verify>rg -rn "播放原声\|显示原文" app/index.html app/static/modules/subtitle-overlay.js</verify>
</task>

<task type="auto">
  <name>Task 8: subtitle-overlay.css — 字幕动画样式</name>
  <files>app/static/css/subtitle-overlay.css</files>
  <read_first>
    - app/static/css/subtitle-overlay.css（当前样式）
  </read_first>
  <action>
新增/修改字幕动画相关样式：

```css
.subtitle-sentence {
  transition: background-color 0.3s ease, transform 0.2s ease, opacity 0.3s ease;
  opacity: 0.7;
}
.subtitle-sentence.is-active {
  opacity: 1;
  transform: scale(1.02);
  background: var(--surface-active, rgba(79, 156, 247, 0.15));
  border-left: 3px solid var(--accent, #4f9cf7);
}
```

删掉 `.subtitle-progress-range` 相关样式（已被波形替代）。
  </action>
  <acceptance_criteria>
    - subtitle-overlay.css 包含新的 `.subtitle-sentence` transition
    - `.is-active` 有 transform 和 border-left 效果
  </acceptance_criteria>
  <verify>grep -n "transition\|is-active\|transform" app/static/css/subtitle-overlay.css</verify>
</task>

</tasks>
