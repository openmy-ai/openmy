---
phase: 01-evidence-based-audio-mapping
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/openmy/services/ingest/transcription_enrichment.py
  - tests/unit/test_transcription_enrichment.py
autonomous: true
requirements:
  - LINK-01
  - LINK-02
user_setup: []
must_haves:
  truths:
    - Scenes with trustworthy single-chunk evidence emit a stable scene-level audio reference derived from evidence offsets
    - Scene audio references never store absolute audio file paths or chunk_path copies
    - Scenes with missing or ambiguous evidence remain without a playable audio reference instead of guessing
  artifacts:
    - src/openmy/services/ingest/transcription_enrichment.py
    - tests/unit/test_transcription_enrichment.py
  key_links:
    - scene.audio_ref is derived from scene.transcription_evidence
    - audio_ref carries stable identifiers and offsets only
---

<objective>
Define the Phase 1 `audio_ref` contract and the helper logic that derives it from existing transcription evidence.

Purpose: Lock the mapping contract before run-path and regression work build on top of it.
Output: Production helper logic plus unit tests for single-chunk, missing-evidence, and multi-chunk cases.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-evidence-based-audio-mapping/01-CONTEXT.md
@.planning/phases/01-evidence-based-audio-mapping/01-RESEARCH.md
@.planning/phases/01-evidence-based-audio-mapping/01-PATTERNS.md
@src/openmy/services/ingest/transcription_enrichment.py
@tests/unit/test_transcription_enrichment.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a scene-level audio reference derivation helper</name>
  <files>src/openmy/services/ingest/transcription_enrichment.py</files>
  <read_first>src/openmy/services/ingest/transcription_enrichment.py, .planning/phases/01-evidence-based-audio-mapping/01-CONTEXT.md, docs/plan-audio-playback.md</read_first>
  <action>Create a focused helper inside `transcription_enrichment.py` that reads a scene's `transcription_evidence` list and returns either a stable `audio_ref` payload or no payload. The helper must accept only one unique non-empty `chunk_id`; compute scene-level `offset_start` and `offset_end` from evidence start/end values; optionally include `segment_ids` or `source`, but do not write `chunk_path` or any absolute path. If evidence is missing, malformed, or spans multiple chunk ids, return no playable ref.</action>
  <verify>python3 -m pytest tests/unit/test_transcription_enrichment.py -v</verify>
  <acceptance_criteria>
    - src/openmy/services/ingest/transcription_enrichment.py contains the string "audio_ref"
    - src/openmy/services/ingest/transcription_enrichment.py does not add "chunk_path" under audio_ref construction
    - tests/unit/test_transcription_enrichment.py contains "test_apply_transcription_enrichment_to_scenes_adds_audio_ref_from_single_chunk_evidence"
    - tests/unit/test_transcription_enrichment.py contains "test_apply_transcription_enrichment_to_scenes_skips_audio_ref_when_evidence_spans_multiple_chunks"
  </acceptance_criteria>
  <done>Audio reference derivation is implemented and covered by targeted unit tests.</done>
</task>

<task type="auto">
  <name>Task 2: Attach the derived audio reference during scene enrichment writeback</name>
  <files>src/openmy/services/ingest/transcription_enrichment.py</files>
  <read_first>src/openmy/services/ingest/transcription_enrichment.py, .planning/phases/01-evidence-based-audio-mapping/01-RESEARCH.md</read_first>
  <action>Update `apply_transcription_enrichment_to_scenes(...)` so each scene keeps its existing `transcription_evidence` behavior and additionally receives `audio_ref` when the derivation helper returns a trustworthy result. Preserve current `speaker_hints` logic. If a scene becomes unavailable for playback, omit `audio_ref` rather than storing guessed or path-based data.</action>
  <verify>python3 -m pytest tests/unit/test_transcription_enrichment.py -v</verify>
  <acceptance_criteria>
    - src/openmy/services/ingest/transcription_enrichment.py contains "scene[\"audio_ref\"]" or "scene['audio_ref']"
    - src/openmy/services/ingest/transcription_enrichment.py still contains "scene[\"transcription_evidence\"]" or "scene['transcription_evidence']"
    - src/openmy/services/ingest/transcription_enrichment.py still uses "safe_write_json"
  </acceptance_criteria>
  <done>Scene enrichment writes stable audio refs without changing the existing evidence-chain ownership model.</done>
</task>

<task type="auto">
  <name>Task 3: Add helper-level regression coverage for safe-fail cases</name>
  <files>tests/unit/test_transcription_enrichment.py</files>
  <read_first>tests/unit/test_transcription_enrichment.py, src/openmy/services/ingest/transcription_enrichment.py, .planning/phases/01-evidence-based-audio-mapping/01-PATTERNS.md</read_first>
  <action>Extend the transcription enrichment test file with scene-writeback cases that cover: one chunk with valid offsets, missing evidence, and evidence spanning multiple chunks. Assert on the resulting `scenes.json` payload fields directly so future refactors cannot reintroduce path storage or silent guessing.</action>
  <verify>python3 -m pytest tests/unit/test_transcription_enrichment.py -v</verify>
  <acceptance_criteria>
    - tests/unit/test_transcription_enrichment.py contains "audio_ref"
    - tests/unit/test_transcription_enrichment.py contains "offset_start"
    - tests/unit/test_transcription_enrichment.py contains "offset_end"
    - tests/unit/test_transcription_enrichment.py contains "chunk_path" only in fixture setup or negative assertions, not as expected audio_ref output
  </acceptance_criteria>
  <done>Unit coverage locks the audio_ref contract and safe-fail behavior.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python3 -m pytest tests/unit/test_transcription_enrichment.py -v`
- [ ] `rg -n "audio_ref|offset_start|offset_end" src/openmy/services/ingest/transcription_enrichment.py tests/unit/test_transcription_enrichment.py`
- [ ] Confirm no `audio_ref` field stores `chunk_path` or an absolute path
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 1 has a stable, test-locked `audio_ref` contract rooted in `transcription_evidence`
  </success_criteria>

<output>
After completion, create `.planning/phases/01-evidence-based-audio-mapping/01-01-SUMMARY.md`
</output>
